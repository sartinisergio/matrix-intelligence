[build]
  command = "node build-netlify.mjs"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# I redirect sono in dist/_redirects (generato da build-netlify.mjs)
# Più affidabili del netlify.toml per proxy a Functions

[dev]
  command = "npm run dev"
  port = 3000
